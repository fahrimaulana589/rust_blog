-- ALTER TABLE portofolios ADD COLUMN slug TEXT NOT NULL DEFAULT '';
-- UPDATE portofolios SET slug = 'slug-' || hex(randomblob(4));
-- CREATE UNIQUE INDEX portofolios_slug_unique ON portofolios(slug);
SELECT 1;
